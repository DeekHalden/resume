'use strict';

/**
 * @ngdoc function
 * @name resumeApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the resumeApp
 */
/*angular.module('resumeApp')
    .constant("baseURL", "https://localhost:3443/")
    .factory('homeFac', ['$resource', 'baseURL', function($resource, baseURL) {
        return $resource(baseURL + 'info/', null, {
            'update': {
                method: "GET"
            }
        })

    }])*/
